# Minimal requirements for Render deployment
# Using older, stable versions with pre-built wheels

# Core AI (using older versions that compile cleanly)
langchain==0.0.350
langchain-community==0.0.9
groq==0.4.1

# Data validation (stable version)
pydantic==2.4.2
pydantic-settings==2.0.3
python-dotenv==1.0.0

# Web framework (older stable versions)
fastapi==0.100.1
uvicorn==0.23.2

# Logging
structlog==23.2.0

# External integrations (optional - only install if needed)
python-telegram-bot==20.7
twilio==8.12.0

# Legacy fallback
langchain-openai==0.0.8
openai==1.3.8
