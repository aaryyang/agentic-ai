# FINAL RENDER-COMPATIBLE REQUIREMENTS
# Using newer versions compatible with Python 3.13 and numpy 2.x

# Core AI (newer versions that work with numpy 2.x)
langchain==0.3.0
langchain-groq==0.2.0
groq==0.4.1

# Data validation (compatible versions)
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.0

# Web framework
fastapi==0.112.0
uvicorn[standard]==0.30.0

# Basic dependencies
requests==2.31.0
structlog==24.4.0
