# RENDER DEPLOYMENT - LANGCHAIN COMPATIBLE
# Fixed dependency resolution for Python 3.13

# Core AI Framework (Compatible versions)
langchain==0.2.16
langchain-groq==0.1.9
groq==0.9.0

# Data Validation (Stable)
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
python-dotenv==1.0.0

# Web Framework
fastapi==0.112.0
uvicorn[standard]==0.30.0

# Core Dependencies (Compatible with above)
requests==2.31.0
structlog==24.4.0

# Allow pip to resolve numpy automatically
# (LangChain will install compatible version)
